{
  "installCommand": "npm install && cd \"Kimi_Agent_Exact Website Copy Request/server\" && npm install && npx prisma generate && cd ../app && npm install",
  "buildCommand": "cd \"Kimi_Agent_Exact Website Copy Request/app\" && npm run build",
  "outputDirectory": "Kimi_Agent_Exact Website Copy Request/app/dist",
  "framework": null,
  "functions": {
    "api/**/*.ts": {
      "maxDuration": 30,
      "includeFiles": "Kimi_Agent_Exact Website Copy Request/server/src/**,Kimi_Agent_Exact Website Copy Request/server/prisma/**"
    }
  },
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/[...path].ts" },
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
